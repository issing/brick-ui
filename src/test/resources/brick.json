{
    ui : {
        test : {
            screens : [ {
                path : "net.isger.brick.test.ui"
            } ]
        }
    },
    
    plugin : {
        test : {
            screens : [ {
                path : "net.isger.brick.test.screen"
            } ],
            services : [ {
                path : "net.isger.brick.test.service"
            } ],
            persists : [ {
                path : "net.isger.brick.test.persist"
            } ]
        }
    }
}
